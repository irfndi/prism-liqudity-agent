{
  "name": "@prism-liquidity-agent/cloudflare",
  "version": "1.0.0",
  "description": "Cloudflare Workers for Prism DLMM platform",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "db:migrate": "wrangler d1 migrations apply prism-db",
    "db:create": "wrangler d1 create prism-db",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "^3.21.2",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.12",
    "@cloudflare/workers-types": "^4.20250214.0",
    "@vitest/runner": "^4.1.0",
    "@vitest/snapshot": "^4.1.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.96.0"
  }
}
